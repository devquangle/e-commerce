spring:
  application:
    name: backend

  profiles:
    active: dev

  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB

  output:
    ansi:
      enabled: ALWAYS