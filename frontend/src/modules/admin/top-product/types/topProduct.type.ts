export type TopProductItem = {
  rank: number;
  name: string;
  category: string;
  sold: number;
  revenue: string;
  growth: string;
  stock: number;
};

export type CategoryDistributionItem = {
  name: string;
  percent: number;
};
